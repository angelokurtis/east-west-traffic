version: '3.7'

services:
  client:
    image: 'kurtis/client:latest'
    ports:
      - 8080:8081
    environment:
      - GRPC_ADDRESS=server
      - GRPC_PORT=6565

  server:
    image: 'kurtis/server:latest'
    environment:
      - GRPC_PORT=6565